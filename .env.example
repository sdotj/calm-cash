# Copy to .env for local overrides if needed.

POSTGRES_USER=budget
POSTGRES_PASSWORD=budget
POSTGRES_DB=budget_dev

# Shared JWT secret for local development (change for real environments)
JWT_SECRET=dev-placeholder

